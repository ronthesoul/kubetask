apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: 192.168.49.1
    path: /nfs
  storageClassName: nfs-storage
  persistentVolumeReclaimPolicy: Retain
